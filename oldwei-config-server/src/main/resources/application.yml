
spring:
  application:
    name:  oldwei-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/weizd21/oldwei.git #GitHub上面的git仓库名字
          label: develop
          search-paths: oldwei-config-file
#          username: weizd21
#          password: xxxxxxx

server:
  port: 8002


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka
